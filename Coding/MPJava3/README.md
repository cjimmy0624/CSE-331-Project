# CSE-331-Project
- We can change bandwidth AT A COST of $2 per inc. in bandwidth
X (total inc. of bandwidth) = Summation (max(0,b_u' - b_u))
 - Can only increase or keep bandwidth

B_c: complaint threshold
- B_c <= a_c: can complain before unsubscribing
